box: ubuntu:14.04

build:
  steps:
    - script:
        name: update environment
        code: |
          apt-get -y update
          apt-get -y upgrade

    - script:
        name: install mysql
        code: |
          apt-get install mysql-server -y
          mysql --version
          sudo service mysql start
          ps auxf | grep mysql
